{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:csr && npm run build:ssr",
    "build:ssr:watch": "vite build --watch",
    "build:csr:watch": "vite build --mode client --watch",
    "build:ssr": "vite build",
    "build:csr": "vite build --mode client",
    "preview": "wrangler pages dev dist",
    "deploy": "$npm_execpath run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@generouted/react-router": "^1.18.5",
    "hono": "^4.1.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "@hono/vite-cloudflare-pages": "^0.2.4",
    "@hono/vite-dev-server": "^0.9.0",
    "@types/react": "^18.2.67",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.4.2",
    "vite": "^5.0.12",
    "vite-plugin-pages": "^0.32.0",
    "wrangler": "^3.32.0"
  }
}
